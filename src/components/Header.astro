---
import { GITHUB_URL, LINKEDIN_URL } from '../consts';
import { getProjectsByCategory } from '../data/projects';

const personalProjects = getProjectsByCategory('personal');
const teamProjects = getProjectsByCategory('team');
---

<header>
	<nav>
		<h2><a href="/" data-i18n="nav.brand">SHANGLI ZHOU</a></h2>
		<div class="internal-links">
			<a href="/#about" data-i18n="nav.about">About</a>
			<a href="/#experience" data-i18n-inline data-en="Experience" data-zh="经历">Experience</a>

			<div class="nav-dropdown">
				<a
					href="/#personal-projects"
					data-i18n-inline
					data-en="Personal Projects"
					data-zh="个人项目">Personal Projects</a
				>
				<div class="dropdown-panel">
					{
						personalProjects.map((project) => (
							<a class="dropdown-link" href={`/projects/${project.slug}`}>
								{project.title}
							</a>
						))
					}
				</div>
			</div>

			<div class="nav-dropdown">
				<a href="/#team-projects" data-i18n="nav.teamwork">Team Projects</a>
				<div class="dropdown-panel">
					{
						teamProjects.map((project) => (
							<a class="dropdown-link" href={`/projects/${project.slug}`}>
								{project.title}
							</a>
						))
					}
				</div>
			</div>

			<a class="external-link" href={LINKEDIN_URL} target="_blank" rel="noreferrer" data-i18n="nav.linkedin"
				>LinkedIn</a
			>
			<a class="external-link" href={GITHUB_URL} target="_blank" rel="noreferrer" data-i18n="nav.github"
				>GitHub</a
			>
			<a class="external-link" href="/documents/resume-placeholder.pdf" target="_blank" data-i18n="nav.resume"
				>Resume</a
			>
			<button
				class="language-toggle"
				type="button"
				data-language-toggle
				data-i18n-aria-label="nav.languageToggleLabel"
				aria-label="Switch language to Chinese">
				<span class="language-dot"></span>
				<span data-i18n="nav.languageToggle">中文</span>
			</button>
		</div>
	</nav>
</header>
<style>
	header {
		position: sticky;
		top: 0;
		z-index: 10;
		border-bottom: 1px solid rgba(241, 234, 210, 0.26);
		background:
			linear-gradient(180deg, rgba(255, 255, 255, 0.055), transparent 38%),
			rgba(12, 12, 15, 0.86);
		box-shadow:
			inset 0 1px 0 rgba(255, 255, 255, 0.08),
			inset 0 -1px 0 rgba(241, 234, 210, 0.24),
			0 12px 30px rgba(0, 0, 0, 0.38),
			0 0 26px rgba(241, 234, 210, 0.08);
		backdrop-filter: blur(18px);
	}
	header::after {
		position: absolute;
		right: 0;
		bottom: -1px;
		left: 0;
		height: 1px;
		background: linear-gradient(
			90deg,
			transparent,
			rgba(241, 234, 210, 0.72),
			rgba(255, 248, 230, 0.48),
			transparent
		);
		content: '';
		pointer-events: none;
	}
	h2 {
		margin: 0;
		font-size: 1.35rem;
		letter-spacing: 0;
		white-space: nowrap;
	}

	h2 a,
	h2 a.active {
		color: var(--text);
		text-decoration: none;
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1.5rem;
		width: min(1180px, calc(100% - 2rem));
		margin: 0 auto;
		min-height: 72px;
	}
	nav a {
		color: var(--muted);
		font-size: 1.1rem;
		text-decoration: none;
		text-shadow: 0 1px 0 rgba(0, 0, 0, 0.45);
	}
	nav a.external-link {
		text-decoration: underline;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.22em;
	}
	button.language-toggle {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		min-width: 58px;
		min-height: 42px;
		padding: 0.45rem 0.85rem;
		border: 1px solid rgba(241, 234, 210, 0.42);
		border-radius: 8px;
		background:
			linear-gradient(180deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0.015)),
			rgba(22, 22, 28, 0.72);
		color: var(--text);
		box-shadow:
			inset 0 1px 0 rgba(255, 255, 255, 0.1),
			0 0 18px rgba(241, 234, 210, 0.12);
		font: inherit;
		font-size: 1.05rem;
		cursor: pointer;
	}
	.language-dot {
		width: 0.42rem;
		height: 0.42rem;
		border-radius: 999px;
		background: #f1ead2;
		box-shadow: 0 0 10px rgba(241, 234, 210, 0.7);
	}
	nav a:hover,
	nav a.active,
	button.language-toggle:hover {
		color: var(--text);
		border-color: rgba(255, 248, 230, 0.62);
		text-shadow: 0 0 14px rgba(241, 234, 210, 0.42);
	}
	.internal-links {
		display: flex;
		align-items: center;
		justify-content: flex-end;
		gap: clamp(0.7rem, 2vw, 1.5rem);
		flex-wrap: wrap;
	}
	.nav-dropdown {
		position: relative;
		display: flex;
		align-items: center;
	}
	.dropdown-panel {
		position: absolute;
		top: calc(100% + 0.75rem);
		left: 0;
		z-index: 20;
		display: grid;
		min-width: 220px;
		padding: 0.8rem 0;
		border: 1px solid rgba(241, 234, 210, 0.28);
		background:
			linear-gradient(180deg, rgba(255, 255, 255, 0.06), transparent 32%),
			rgba(18, 18, 23, 0.98);
		box-shadow:
			inset 0 1px 0 rgba(255, 255, 255, 0.09),
			0 16px 30px rgba(0, 0, 0, 0.45),
			0 0 24px rgba(241, 234, 210, 0.16);
		opacity: 0;
		pointer-events: none;
		transform: translateY(-4px);
		transition:
			opacity 150ms ease,
			transform 150ms ease;
	}
	.nav-dropdown:hover .dropdown-panel,
	.nav-dropdown:focus-within .dropdown-panel {
		opacity: 1;
		pointer-events: auto;
		transform: translateY(0);
	}
	.dropdown-panel a.dropdown-link {
		padding: 0.65rem 1rem;
		color: var(--muted);
		white-space: nowrap;
	}
	.dropdown-panel a.dropdown-link:hover {
		color: var(--text);
		background: rgba(255, 255, 255, 0.04);
	}
	@media (max-width: 720px) {
		nav {
			align-items: flex-start;
			flex-direction: column;
			padding: 1rem 0;
		}
		.internal-links {
			justify-content: flex-start;
		}
		.nav-dropdown {
			align-items: flex-start;
			flex-direction: column;
		}
		.dropdown-panel {
			position: static;
			min-width: 0;
			padding: 0.25rem 0 0 0.75rem;
			border: 0;
			background: transparent;
			box-shadow: none;
			opacity: 1;
			pointer-events: auto;
			transform: none;
		}
	}
</style>
